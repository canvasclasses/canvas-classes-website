# PROJECTS — the cockpit

> **Read this FIRST, at the start of every session.** It is the single air-traffic-control view of every active initiative: where each one stands, what to do next, and — most importantly — **what each one is blocked on**. You run many fronts in parallel and lose context when you switch; this file is the antidote. You should never have to remember "where was I?" — it's here.
>
> **This file is agent-maintained.** Per CLAUDE.md §0.5 (Project Ledger Protocol), the agent surfaces the relevant row when you return to a project (check-in) and updates the row + the project's detail doc + GBrain when work advances (check-out). If you ever find a row stale, that's a bug in the ritual — tell the agent to fix it.
>
> Status legend: 🟢 active/shipping · 🟡 in progress · ⛔ blocked (waiting on a dependency) · 💤 idle (untouched a while) · ✅ done. **"Next action" is always the single next concrete step.**

_Last reviewed: 2026-06-19_

## Cockpit

| Project | Status | Phase / where it is | Last touched | Next action | Blocked on |
|---|---|---|---|---|---|
| **Chemistry Flashcards Quality Pass** | 🟡 | **Audit + cleanup + 8 chapters authored AND rewritten in punchy style, 2026-06-18.** Cleanups: 289 `$$..$$` LaTeX rewritten; 18 duplicate-question groups soft-deleted; 195 `?.` artifacts stripped. UI fix: answer-side now left-aligned (was center-aligned, broke for multi-line answers) — [FlashcardsClient.tsx:910](apps/student/features/flashcards/components/FlashcardsClient.tsx#L910) + [FlashcardsChapterClient.tsx:522](apps/student/features/flashcards/components/FlashcardsChapterClient.tsx#L522). Discoverability: "Flashcards" CTA badge on every Crucible chemistry chapter page header. **400 final cards across all 8 Class-11 chapters**, all in the second-pass punchy style (founder feedback after Periodicity v1 showed paragraph answers don't work as flashcards): Mole Concept (87), Ionic Equilibrium (63), Chemical Equilibrium (39), Chemical Bonding (58), Thermodynamics (35), Redox (24), **Periodicity (60, moved to Inorganic category)**, Hydrocarbons (34). All cards: avg answer length 94–148 chars (down from 300–500+ in v1); bold key answer first; tables/bullets over paragraphs; PYQ-inspired tricky framings; conceptual-gap focus; real easy/medium/hard difficulty mix. ID ranges: FLASH-PHY-1100..1623 + FLASH-INORG-0957..1016 + FLASH-ORG-1100..1133 (all v1 cards soft-deleted, rollback snapshots in `_agents/snapshots/`). **Diagrams wishlist:** Notion DB [🖼️ Chemistry Flashcards — Diagrams Wishlist](https://app.notion.com/p/7af26ad2028a47098b1567a0f74af465) now contains 19 v1 entries (obsolete — point at deleted IDs) + 18 v2 entries (current). | 2026-06-18 | **(1) Founder spot-check rewritten chapters (especially compare to Periodicity v2 which set the bar).** **(2) Founder: clean obsolete v1 entries from Notion wishlist** (those pointing at FLASH-PHY-04xx-07xx ranges — superseded by v2 entries at 11xx-16xx; v2 entries note "Replaces old ..."). **(3) Produce High-priority diagrams** from the wishlist. **(4) Re-grade difficulty on the bulk-NCERT decks** (~1,750 cards still all-tagged "medium"). | None — proceed at founder's pace. |
| **Adaptive Persona Engine** | 🟡 | Phase 1 COMPLETE. **Phase 2 BUILT** (persona-snapshot + guardrailed AI tutor `POST /api/v2/user/tutor` + model routing; 14/14 & 16/16 tests). **Phase 3a foundation BUILT 2026-06-07** — tenant dimension stamped from birth: new `tenants`/`tenant_memberships` + `@canvas/data/tenancy` resolver; `tenant_id` on `UserProgress`/`StudentChapterProfile` (set at creation like `user_email`, writer.ts untouched) + every `learning_event`; snapshot v2 carries it. Verified 11/11; landed with 0 untagged legacy data. Both apps typecheck clean. **🔀 DIRECTION CHANGE 2026-06-07 (ADR-012): Live Books DECOUPLED from Crucible recommendation** — engine re-centred on in-Crucible adaptive practice; cross-surface redirect parked (not deleted); solutions become self-sufficient; flagship next feature = question grouping / variant-skipping (deferred until 2–3 chapters/subject done). | 2026-06-07 | **(1) Top up Anthropic credit** → tutor live (Crucible-scoped). **(2) Finish 2–3 fully tagged + solved chemistry chapters.** **(3) Build question grouping / variant-skipping** (design spec then). Phase 3b teacher dashboards as B2B demands. | **⛔ Anthropic credit:** tutor wiring authenticates but live call 400s on "credit too low" — needs top-up. |
| **Class 9 English — Kaveri** | 🟡 | Full content built + competency-deepening pass done (8 ch, 139 pages, Grammar Gyms, Hinglish, practice). **CBSE alignment research done** (verified against primary CBSE PDFs; killed the "40→50% for IX-X" myth) → GBrain `reference/2026-06-07-cbse-class9-assessment-model-and-livebook-roadmap.md`. **`reading_comprehension` block type shipped** (types + Zod schema + renderer + BlockRenderer wired, both typechecks clean). **Ch9 "Reading Skills: Unseen Passages" live** — intro + 4-step attack + discursive 430w passage + case-based 235w passage (with data table); 8 objective Qs spread 3/2/2/1 across A/B/C/D; mixed MCQ / assertion-reason / short-answer; teacher voice ("har jawab passage ke andar hai"). **NEW: `voices_that_inspire` callout variant shipped** (rose/amber warm card with pulled-out italic quote, attribution, reflection prompt) + **"One Page from My Bookshelf" pilot live on Ch1 themes page** (Tagore's *Where the mind is without fear* → Krishtakka). **wisdom_seeds capture pipeline live** at `~/brain/wisdom_seeds/` (README + template + first seed + pilot quality reference at `_pilots/ch1-tagore-grandmother.md`). | 2026-06-07 | Author discursive + case-based passages for ch2-8 (one of each per chapter); seed wisdom_seeds with more books as founder reads and roll out voices_that_inspire to ch2-8; record audio; build Ch2 "About the Poet" + standalone Writing pages | **⛔ Source:** needs the Kaveri PDF for poet bio + exact writing tasks; **⛔ Audio:** all `audio_url` empty (recording workflow) |
| **Class 9 Science — NCERT Simplified** | 🟡 | Ch6 fully built (11pp + 26-Q bank); **all 5 live chapters' inline quizzes fixed to §3.6.1 (232 Qs: length-tell 58→7%, B-bias 57→24%, difficulty 10→90%)**; "Free-Body Challenge" game built (`free-body-diagram`) on Ch6. Toolchains: `scripts/science-quiz/`, `scripts/science-practice/` | 2026-06-06 | Wire the unused physics sims (free-fall/friction/circular-motion etc.) into pages; add chapter-end practice banks to Ch1–5; publish+fix Ch8; Ch6 images + Hinglish | — |
| **Class 9 Math Live Book** | ⛔ | Part I (Ch1–8) COMPLETE, published:false | 2026-06-03 | Build Part II (DB ch 9–15) | **⛔ Source:** waiting on the NCERT PDFs for Part II |
| **Class 9 ICT Live Book** | 🟡 | Ch1 done; 9 sims built + UX v2 pass | 2026-06-04 | Build video-driven chapters 2–5 | Video source material |
| **Class 9 Hindi — Ganga (गंगा)** | 🟡 | All 12 chapters built (103 pp, `published:false`); question-quality pass DONE. **Image generation 90/112 done (Ch1–8 + Ch9 first 5/9), paused on ChatGPT daily image quota — resume after 18:34 IST on 2026-06-07.** CBSE-alignment research done; **अपठित बोध module batch-1 BUILT** (new ch13: 5 pages, 28 Qs MCQ+assertion-reason+short-answer, reuses shared `reading_comprehension` block now `lang:'hindi'`) → `_agents/plans/HINDI_CBSE_EXAM_ALIGNMENT.md`. Remaining gaps: लेखन / व्याकरण / model-papers | 2026-06-07 | (1) After 18:34 IST, finish images: Ch9 #6–#9, Ch10 (7), Ch11 (8), Ch12 (8) + ch13 अपठित (5). (2) Build **रचनात्मक लेखन module** (G2, 20 marks) or extend अपठित, then व्याकरण + model papers | **⛔ (soft)** ChatGPT image quota (until 18:34 IST 2026-06-07); CBSE गंगा-specific SQP not yet published |
| **Class 11 Chemistry Live Book** | 🟡 | 57 pages; Ch1–2 + Practical complete, Ch3 unpublished, Ch4+ not started. **2026-06-14 (later): added 5 founder solved examples to `laws-chemical-combinations`** (Q2 definite-proportions, Q3 multiple-proportions, Q4/Q5 combining-volumes, Q6 Avogadro atom-ratio), each after its section's theory. **NEW competitive page "Significant Figures & Precision"** inserted as Ch.1 sidebar page 6 (after Scientific Measurement; laws→7…h2o2→16 shifted cleanly, verified no collisions). 27 blocks: significance of measurement → precision/accuracy → sig-fig counting/calc/rounding rules (plain language) → 4 solved examples → founder's practice set (Q1/Q2/Q3/Q8 tap-to-reveal + Q5/Q6 MCQ). Marked **competitive** (not NCERT). **`published:false`** pending founder review + 3 image gens. Also ended page 4 with a Measurement-of-Properties intro. ⚠ Q2-E "18 students" set to rule-correct "unlimited (exact)" vs founder note's "2". **2026-06-14 (reference-first build, Shishir Mittal §1.x; all via the new content-protection gateway):** RESTORED the hard-deleted "Classification of Matter" section on `introduction-chemistry-matter` (re-linked the founder's ORIGINAL R2 video + added stoichiometric/non-stoichiometric); `how-chemistry-began` gained How Science Works + How to Study Chemistry + How to Use This Book; `scientific-measurement` gained a Length section (no precision instruments); `importance-of-chemistry` gained the Five Branches. **2 images PENDING** (matter-tree, ruler-reading). **2026-06-14: Added a "Pressure and Its Measurement" section to `scientific-measurement` (30→44 blk).** Closes the gap left when **States of Matter was cut from the rationalised NCERT Class 11 Chem syllabus** — pressure is needed for Thermodynamics ($w=-P\Delta V$) but NCERT Physics teaches it only mid-course (Mechanical Properties of Fluids, Part II), not the initial chapters. Section: concept (force/area, pascal) + Pa/kPa/bar/atm/torr·mmHg table + master-chain `remember` callout + barometer (Torricelli) + manometer (gauge vs absolute, $P_\text{abs}=P_\text{atm}+P_\text{gauge}$) + worked example; exam_tip gained the conversion chain + the **R-unit trap** (0.0821 atm vs 8.314 Pa). 2 images (barometer, manometer) PENDING generation. Quiz untouched. **2026-06-13: Ch.1 teacher-voice Pass 1 + 1b + 1c — full chapter swept** (all 15 pages). Pass 1+1b (first ~6 pages): killed AI tells (reveal-frames, drama closers, "not X. It's Y" pairs, "in any dismissive sense", "fingerprints/bookkeeping" dup), de-duped Mars-Orbiter retelling, fixed typos, **filled the reciprocal-proportions gap** on the Laws page. Pass 1c (back half, page_number 6–14, mole-concept run): these calc pages were largely clean of prose tells — fixed objective gaps only: p8 unbalanced `$…$$` KaTeX bug, p13+p14 callouts titled "Real Life Hook" (leaked authoring label) retitled, p14 `\dfrac`→`\frac` (×2 incl. Hinglish twin), p6 "A Mole of Sand" title mismatch retitled. **Quiz pass: all 40 inline_quiz Qs brought to §3.6.1** via the `scripts/science-quiz/` toolchain (book override) — difficulty-tagged 33 untagged Qs, fixed 5 length-tells, killed a banned "All of the above", de-AI'd explanations, balanced key positions to A/B/C/D=10/10/10/10; `quiz_validate.js 1` ✅ PASS. **Content add: expanded the bland density block + new Specific Gravity sub-section** on `scientific-measurement` (28→30 blk; factors affecting density + the specific-gravity-vs-density exam trap, teacher voice). First Live Book to get the teacher voice. **2026-06-10:** Ch.1 dup-page cleanup + `unit-conversion-arena` sim. | 2026-06-14 | **(0) Review + PUBLISH the new Significant Figures page** (sidebar 6, currently DRAFT) and confirm/adjust the Q2-E "18 students" answer. **(0a) Generate the 7 pending Ch.1 images** (barometer, manometer, matter-tree, ruler-reading, + sig-figs page: hero, precision-dartboard, p=number+unit anatomy — prompts ready in the blocks, `src:""`). **(0b) Founder: record the "how to use this book" video** for `how-chemistry-began` after the chapter is complete; verify the restored Classification-of-Matter video plays. **(a) Founder to decide p12 concentration unit count** (page says "seven" in body but "six" in subtitle/callout/image — ambiguous). **(b) Hinglish-twin sync** (§12) for the 3 twins still carrying English tells on the two narrative pages. **(c) Pass 2 (needs sign-off):** confirm Lavoisier 1744 date + reciprocal-proportions-in-body; split the 28-blk measurement + 18-blk properties pages (§15.8) then full voice-rewrite to lock to embedded audio. Optional: re-sort stale `page_ids` array; replace placeholder "ChatGPT Image…" alt-texts. Then continue chapters. | **⏳ Founder:** p12 count + Pass-2 sign-off (Lavoisier date + page-split/voice-rewrite) |
| **Crucible question bank** | 🟢 | 15,226 live questions | 2026-05-31 | Micro-tag Physics/Maths once their taxonomy micro_topics are generated | **⛔ Taxonomy:** Physics & Maths have zero micro_topics in the taxonomy → can't micro-tag yet |
| **NCERT corpus (Chemistry Teaching Brain)** | 🟡 | **Started 2026-06-13.** Repo-resident, citable NCERT index so the solution-writer + book-page pipelines ground content in NCERT's exact wording and cite the source (credibility on NCERT-derived questions). Decision: lives in repo (mirrors `_agents/voice/` — git-diffable improvement loop), **not** GBrain/vector-DB. **Pilot DONE:** `_agents/ncert/chemistry/ATOM-ncert.md` (full Cl-11 Unit 2 ingested via pdftotext + image reads — every §, all constants, 18 worked Problems, exact page provenance). `NCERT_INDEX.md` + ingestion runbook + gitignored `_source/`. **Wired into pipelines:** chemistry-solution-workflow §🎤 (consult+cite step + checklist #14) + BOOK_PAGE_WORKFLOW §5.V (grounding step). Founder's notes (Notability handwritten PDFs, Drive folder) = wave 2 (vision-OCR), pending the link. **CITATIONS LIVE ✅ 2026-06-13:** classified all 299 ATOM Qs (`ATOM-citation-map.md`: 270/299 = 90% citable) + prepended an "As per NCERT … §x.y (p.NN)." lead line as the FIRST line of all 270 Tier-A/B solutions (12 also cite the matching NCERT solved Problem; 29 Tier-C untouched). Idempotent toolkit `scripts/ncert/` + backup/rollback. | 2026-06-13 | **Ingest MOLE next** (official NCERT PDF → `MOLE-ncert.md` + citation map → apply). Wave 2: founder shares Notability Drive link → spec vision-OCR ingestion. Optional: log architecture decision to GBrain. | **⏳ Founder:** share Notability Drive link for wave 2 |
| **Crucible solution-writing (Physics)** | 🟡 | Teacher-style 6-section solutions per `physics-solution-workflow`, via `scripts/math-solutions/` toolkit (fetch→batch→apply). **Done: ELST 203/203, CAPC 123/123, CURR 285/285, MAGM 55/55, Magnetic Effects of Current (MVCH=ph12_moving_charges 193/193 — COMPLETE 2026-06-10, audit-clean).** MVCH: 2 answer-key corrections (166 c→d 1T; 175 a→b eμ₀nIR/2m), 1 empty-answer fill (086 →0.01s), 6 verify flags; ~35 figures read SVG→dark-PNG. **MVCH Notion sync DONE** (166, 175, 086 + 031/118/148 verify flags → Physics Answer Discrepancies DB `0bee1feb-…`). | 2026-06-10 | **Pick the next physics chapter** (e.g. Ray Optics 202, EMI 102, AC 138, Rotational 192 — all 0/N solved). | — (MVCH done; no blocker) |
| **Physics MCQ book ingestion** | 🟡 | *Physics MCQ* (Deb Mukherji) → Crucible, **per-question taxonomy routing** (book chapters don't match ours). Decisions locked 2026-06-09: `sourceType:'Practice'`, `applicableExams:['JEE']`, both Type-1/Type-2, answer keys from founder per chapter. Routing rubric + workflow in memory `project_physics_mcq_book_ingestion`. **Book Ch.1 "Kinematics, Forces" — COMPLETE ✅ 101/101** ingested + DB-verified (K1D 124–138, K2D 094–111, COM 097–121, NLM 133–166, WEP 123–129, SHM 118–119). All `Practice`/`['JEE']`/`null`, published, per-question routing. **Also flipped 674 older physics Qs `review`→`published`** (physics bank live, 3,798). **⚑ 3 key items for founder review:** K2D-107 (Q67 opt c), COM-115 (Q75 opt c), COM-121 (Q84 opt d). Founder uploaded all figures (34/101 inline in markdown). **Ch.1 SOLUTIONS COMPLETE ✅ 101/101** (teacher-style 6-section, DB-sourced, all answers re-derived; `\dfrac`→`\frac` cleanup done; 101/101 structure-valid). **4 answer-key discrepancies found & Notion-pushed:** COM-115 (→b,d), COM-117 (→a,b,c,d), COM-121 (→a,b; avg accel 100 m/s²), K2D-107 (→a,b,c,d; inside frame inertial). **Revision Exercise 1 COMPLETE ✅ 2026-06-10 (ingested + solved)** — the 26-Q single-correct mechanics revision set (book pg 1-49→1-55) routed per-question: COM-122→127 (6), NLM-167→177 (11), K2D-112→116 (5), K1D-139/140 (2), WEP-130/131 (2). All `Practice`/`['JEE']`, published. **All 26 solutions written** (teacher-style 6-section), every answer independently re-derived — **all 26 match the book key, 0 corrections, 0 flags, 0 blocking.** Founder uploaded the 4 figures (NLM-167/169/175/177); read SVG→dark-PNG to confirm (NLM-175→d, NLM-177→c). **Hybrid worked:** solutions seeded from `_agents/solution-seeds/physics-mcq-rev1.md` (book's Hints), no PDF re-read. No Notion sync (clean). **13 solution-side diagrams queued** to Notion Wishlist DB (9 the book drew + 4 FBD candidates: K2D-112/113/114/115, K1D-140, NLM-171/173/174/176/168/172, COM-124/127) — founder draws+uploads, then embed URLs into solutions. **Book Ch.2 "Circular, Rotational & SHM Motions" — INGESTED ✅ 2026-06-10, all 100 (Q1–Q100, pg 1-63→1-89).** Per-question routing: NLM 29 (circular dynamics, tag_nlm_6 → NLM-178→206), ROT 56 (→ROT-194→249), SHM 9 (→SHM-120→128), COM 6 (CM Qs → COM-128→133). Founder rule: circular kinematics→K2D / dynamics→NLM (all Ch.2 circular = dynamics ⇒ 0 K2D; earth-rotation Q44/63/64→ROT ⇒ 0 GRAV). Validator+insert 100/100. **Hybrid solution capture done**: book's Hints (pg 1-91→1-110, ~80/100 solved) → sidecar `solution-seeds/physics-mcq-ch2.md` + qmap `physics-mcq-ch2-qmap.md` (Phase-4 reads sidecar, NOT PDF). **Figure lists for founder**: `_agents/solution-flags/physics-mcq-ch2-diagram-wishlist.md` — List A = 7 question-side REQUIRED (NLM-188/190/201/205, ROT-242/244/245), List B = ~40 solution-side. **3 borderline routings flagged**: NLM-202/203/204 (rotating-body internal tension, alt ROT), ROT-220 (satellite, alt GRAV). **Ch.2 SOLUTIONS COMPLETE ✅ 2026-06-11** — all 100 written (teacher-style 6-section, seeded from the sidecar + 9 figure PNGs, 0 PDF re-reads; every answer independently re-derived, **all 100 match the book key — 0 corrections, 0 blocking, 0 failed validation**; audit clean for NLM/ROT/SHM/COM). **3 verify flags** (likely book typos, flagged not changed): ROT-223 opt (d) 5/2→5/3, ROT-241 opt (c) angle 60°-horiz should be 30°-horiz, ROT-244 opt (b) friction direction. Also removed the stale 800-char floor from `audit.js` to match apply-batch (no-min-length decision). **Ch.2 question-side figures ATTACHED ✅ 2026-06-11** — 14 SVGs → 14 Qs via `figure-inserter`/svg-mapper (7 REQUIRED NLM-188/190/201/205, ROT-242/244/245 + 7 optional NLM-185, SHM-122/124, ROT-226/236/240/246; all single-figure stems, no option-images → no order to verify; clean 14/14; rollback written; R2 keys in corrected layout). **Ch.2 List-B SOLUTION-side figures ATTACHED ✅ 2026-06-11** — 34 SVGs (book-Q#-named, mapped via qmap; grouped `2,3,4`/`16,17`) → 37 Qs; 26 into the solution's existing 🖼️ section, **11 into newly-created 🖼️ sections** (author had skipped 🖼️; inserter created it after 🧠/before 🗺️ holding just the figure — no caption, optional re-pass). 34 new / 3 reused assets, rollback written. **Book Ch.3 "Properties of Matter, Fluids" — INGESTED ✅ 2026-06-11, all 84 (Q1–84, pg 1-111→1-131).** Fans out per-question: **GRAV 33** (160→192: g-variation/rotation, satellites, escape velocity, shell/sphere potential, Kepler, Gauss-for-gravity, double star), **SOLD 7** (083→089: elasticity), **FLUI 38** (132→169: surface tension, viscosity, buoyancy, pressure-under-accel, Bernoulli/Torricelli), **SHM 6** (129→134: two-chapter spillovers). **New founder routing rule (2026-06-11): a two-chapter question goes to the chapter LATER in the sequence** — so earth-rotation apparent-weight Qs → GRAV (confirmed), and the 6 SHM-result spillovers (Q5/14/15/22/83/84) → SHM. Q22 (SOLD+SHM) auto-applied to SHM, flagged for veto. All `Practice`/`['JEE']`/`examDetails:null`, published; validator+insert 84/84. **Hybrid sidecar done**: book Hints → `_agents/solution-seeds/physics-mcq-ch3.md` + qmap (Phase-4 reads sidecar, not PDF). **Figure wishlist** `_agents/solution-flags/physics-mcq-ch3-diagram-wishlist.md`: List A = 8 REQUIRED question-side (GRAV-170, FLUI-138/149/152/161/165/167, SOLD-087) + 11 recommended; List B = 14 solution-side. **Ch.3 SOLUTIONS COMPLETE ✅ 2026-06-11** — all 84 written (teacher-style 6-section, seeded from the sidecar, 0 PDF re-reads; every answer independently re-derived, **all 84 match the book key — 0 corrections, 0 blocking, 0 failed validation**). 11 figure-dependent solutions carry verify flags (GRAV-170, SOLD-087, FLUI-138/145/149/152/161/164/165/167/168) to confirm once figures are uploaded. **Ch.3 question-side figures ATTACHED ✅ 2026-06-11** — 26 SVGs → 17 Qs via the new `scripts/svg-mapper/` pipeline (dark-bg gallery mapper + manifest + `insert-from-manifest.js`: R2 upload + Asset doc + markdown embed; rollback file written). All 8 REQUIRED + 9/11 recommended question figures done (FLUI-134 & FLUI-145 not in the founder's folder); 3 option-image questions had placeholder text replaced by images; option a/b/c/d order book-verified against Q12/Q30/Q74 (caught + fixed two scrambled cases: GRAV-170 c/d, FLUI-161 b/d). **Ch.3 List-B SOLUTION-side figures ATTACHED ✅ 2026-06-11** — 15 SVGs (book-Q#-named) → 16 Qs; 11 into existing 🖼️ sections, 5 created (FLUI-146/150/162/163/169), no captions. **Ch.3 now fully complete (questions + question-figures + all 84 solutions + solution-figures).** | 2026-06-13 | **Heat & Thermo + Sound + Optics + Electrostatics + Electric Current + Electromagnetism + Motion of Charged Particles + Modern Physics + Experimental Physics COMPLETE ✅ end-to-end. PHYSICS SOLUTIONS NOW ON FORMAT v2 (teacher voice — Modern Physics was the pilot). All numbered chapters DONE; now in Part 8 "Miscellaneous Questions" — **Misc Qs-1 COMPLETE ✅ 2026-06-13 (54 Qs, 20-chapter per-question fan-out).** Next: Misc Qs-2 (footer 8-31), then Misc-3/4 + Part-9 practice papers.** **(a) Ch.3 figures DONE (question + solution-side, svg-mapper).** Remaining: add the 2 missing question-side figs (FLUI-134, FLUI-145); confirm Q22→SHM; optional caption pass for the created solution-side sections (5 in Ch.3 + 11 in Ch.2). **(b)** Carryover: Ch.2 COMPLETE (questions + question-figures + solution-side figures + all 100 solutions ✅) — remaining = 3 Ch.2 borderline routings + push 3 Ch.2 verify flags (ROT-223/241/244) to Notion; 13 Rev-Ex-1 diagrams; review 4 flagged Ch.1 keys (COM-115/117/121, K2D-107). **(c)** Next: run svg-mapper over the remaining chapter folders in "Out - Ready for Database"; then Ch.4. **Revision Exercise 2 — COMPLETE ✅ 2026-06-11** (full end-to-end in one session: 27 SCQ ingested per-question-routed → question figures R7/R24 → 27 six-section solutions, all re-derived 0-correction → 17 solution-side figures; audit 27/27 clean; qmap `physics-mcq-rev2-qmap.md`). **Book Ch. "Heat and Thermodynamics" — COMPLETE ✅ 2026-06-11 (full end-to-end in one session, all 4 phases):** 87 questions (Q1–87, footer pg 2-3→2-27) fanned per-question across **THPR 42** (086→127), **TDYN 28** (155→182), **KTG 17** (128→144) — read PDF pages as images since pdftotext strips α/γ/subscripts; founder-approved routing incl. 9 borderlines (Q15/20/22/23/26/28/31/47/66, incl. the Q22→TDYN / Q23→KTG "previous-question" split). All `Practice`/`['JEE']`/`examDetails:null`/published; validator 87/87; one mixed-prefix insert; type by #-correct (67 SCQ/20 MCQ). **Question figures:** founder's `manifest.json` (book-Q#+slot) remapped to display_id; 34 SVGs → 15 Qs; **all 5 option-image Qs (Q27/53/58/59/60) book-verified for a/b/c/d order — founder's order correct, incl. intentional Q53/Q60 generation-order swaps** (no fixes needed). **All 87 six-section solutions written** (every answer independently re-derived → **all 87 match the book key, 0 corrections, 0 flags, 0 blocked**; 87/87 structurally valid). **5 solution-side figures** (39→THPR-102, 54→TDYN-169, 55→TDYN-170, 74→THPR-114, 76→THPR-116) embedded into existing 🖼️ sections (0 created). Q74 "P at 800°C" confirmed NOT a typo (P externally heated; ans 1/9 m matches key). All rollbacks written; CDN 200. Notion sync N/A (0 discrepancies, all figures attached). qmap `physics-mcq-heatthermo-qmap.md`. **Book Ch. "Sound" — COMPLETE ✅ 2026-06-12 (end-to-end; NO solution-side diagrams in this chapter).** 53 questions (Q1–53, Part 3 pg 3-3→3-15) → **single-chapter map to WAVE** (ph11_waves "Waves and Sound"), **WAVE-118→170**. All `Practice`/`['JEE']`/`examDetails:null`/published; validator 53/53; 37 SCQ / 16 MCQ; tags `tag_waves_1..5`. Read PDF as images. Every answer re-derived — all match key **EXCEPT Q48 (WAVE-165)**: book b,c (254 & 258 Hz) but only **c (258 Hz)** is physically consistent (tension↑ raises string freq, so 254 gives fewer beats) — **overrode b,c→c** (answer + is_correct; backup saved) + **pushed to Physics Answer Discrepancies Notion** (Status=Open). **Question figures:** Q51 (WAVE-168) 4 option-graphs (a/b/c/d order book-verified, ans d=step-down) + Q53 (WAVE-170) rotating-sun stem (5th SVG, added — wasn't in founder manifest). **All 53 six-section solutions written** (53/53 valid). Cross-topic kept in WAVE: Q7/44 (thermal/KTG), Q52/53 (Doppler-of-light+Wien). qmap `physics-mcq-sound-qmap.md`. **Book Ch.4 "Optics" — COMPLETE ✅ 2026-06-12 (end-to-end, all 5 phases incl. solution-side diagrams).** 63 questions (Q1–63, Part 4 pg 4-3→4-19) fanned per-question (founder-approved): **ROPY 50** (203→252), **WVOP 13** (118→130) — clean ray/wave split. All `Practice`/`['JEE']`/`examDetails:null`/published; validator 63/63; 41 SCQ / 22 MCQ; tags rop_1..6 / wvop_1..5. Read PDF as images. Every answer re-derived → **all match the book key.** Q4 (ROPY-206) Brewster options `tan^-1(i)`→`tan i` (OCR fix). Borderline Q26/Q59→WVOP (approved). **Question figures:** 10 SVGs → 7 Qs (stems Q1/12/36/39/44/49 + Q37 option-graphs; Q37 a/b/c/d order book-verified, ans a). **All 63 six-section solutions** (63/63 valid). **7 solution-side figures** (3/5/14/36/42/51/52 → ROPY-205/207/216/231/237/246/247) into existing 🖼️ sections. Rollbacks written; CDN live. Notion N/A (0 discrepancies). qmap `physics-mcq-optics-qmap.md`. **Book Part 5 Ch.1 "Electrostatics" — COMPLETE ✅ 2026-06-12 (end-to-end, all 5 phases).** 101 questions (Q1–101, footer pg 5-3→5-29, phys 260-286) fanned per-question on the **NCERT split** (founder-approved): **ELST 38** (204→241: Coulomb/field/dipole/flux-Gauss) + **CAPC 63** (124→186: potential/equipotential/conductors/capacitors/combos/dielectrics). All `Practice`/`['JEE']`/`examDetails:null`/published; validator 101/101; 61 SCQ / 40 MCQ. Founder rulings: SHM-in-field (Q35/36/60/62/79)→ELST (later-in-seq tie-break); plate-conductors (Q70/82/83/84/88)→CAPC tag_capc_3. Read PDF as images. **Every answer independently re-derived → all 101 match the book key, 0 corrections, 0 flags, 0 blocking.** **Question figures:** 19 Qs (3 option-image graph Qs ELST-210/211+CAPC-132 — a/b/c/d order book-verified, answer-figure lands on stored correct option; manifest's mislabeled '1'/'2'→Q7/Q8 remapped; 16 stem figures). **All 101 six-section solutions** (seeded from sidecar `physics-mcq-electrostatics.md`; ELST 241/241 + CAPC 186/186 audit-clean, 0 failed validation). **Solution figures:** 20 Qs (16 SVGs, 3 grouped) into existing 🖼️ sections. qmap `physics-mcq-electrostatics-qmap.md`. Notion N/A. **Book Part 5 Ch.2 "Electric Current in Conductors" — INGEST + Q-FIGURES + ALL 95 SOLUTIONS DONE ✅ 2026-06-12; solution-side figures PENDING (founder folder empty).** 95 questions (Q1–95, footer pg 5-56→5-83) **single-chapter → CURR** (founder-approved, like Sound→WAVE), **CURR-286→380**. RC/capacitor cluster (Q40-48, 62, 86-95) kept in CURR (later-in-seq tie-break; no RC tag in either chapter). All `Practice`/`['JEE']`/`examDetails:null`/published; validator 95/95; 59 SCQ / 36 MCQ. **All answers re-derived — all match key EXCEPT Q95 (CURR-380): OVERRIDDEN c,d→b,d** (same battery+equal R ⇒ equal initial discharge current; backup saved; **Notion-pushed**, Status=Open). **1 verify flag CURR-364 (Q79)** (book marks all 4; option (b) 'εi−i²r' inconsistent w/ standard chemical=εi — flagged not overridden, **Notion Under Review**). **Question figures:** 54 SVGs → 39 Qs (5 option-image Qs Q14/31/40/66/91 a/b/c/d order book-verified; **NO founder manifest — mapped all 54 by viewing rasterized PNGs vs book**; found Q49/Q50 also figured + Q54/Q56 generated out-of-order). **All 95 six-section solutions** (sidecar `physics-mcq-current.md`; CURR 380/380 audit-clean, 0 failed validation). **Phase 5 DONE ✅ 2026-06-12** — founder populated the solution-diagram folder; 15 solution-side SVGs (book-Q# 9/10/18/23/24/25/27/28/32/53/54/60/73/80/85) embedded (10 into existing 🖼️, 5 created); CDN 200; rollback written. **CURR chapter now COMPLETE end-to-end** (questions + Q-figs + 95 solutions + solution-figs). qmap `physics-mcq-current-qmap.md`. **Book Part 5 Ch.3 "Electromagnetism" — COMPLETE end-to-end ✅ 2026-06-13 (all 5 phases).** 123 questions (Q1–123, footer pg 5-132→5-143) **4-way fanned (founder-approved):** **EMW 2** (001–002: EM-wave constants), **MVCH 58** (194–251: Biot-Savart/Ampère/force/torque on moving charges & conductors), **EMI 51** (103–153: induction/Lenz/motional-emf/LR/mutual-L), **ACUR 12** (139–150: LC oscillation + AC/LCR). All `Practice`/`['JEE']`/`examDetails:null`/published; validator 123/123; 85 SCQ / 38 MCQ. Read PDF as images; Phase-4 solutions seeded from sidecar `physics-mcq-electromagnetism.md` (0 PDF re-reads). **Cross-check found 6 key issues, all fixed in DB** (backup `scripts/_backup_emag_keyfix.json`): 2 our-transcription fixes (MVCH-201 c→d, MVCH-221 a→b), 1 OCR-garbled-options reconstruction (ACUR-148→b, P1=P2>P3), **3 genuine book-vs-physics discrepancies Notion-pushed → EMI-150 (Q120, Open: book abd→ad, 180° flux reversal ⇒ option-b wrong), EMI-152 (Q122, Open: book ad→bd, rotating coil = AC generator), EMI-132 (Q66, Under Review: 'no current' wording ⇒ b=φ²/2L vs a=0 ambiguous)** + ACUR-148 logged Resolved. **Question figures:** founder manifest (53 SVGs→44 Qs, book-Q#-keyed) remapped to display_ids + embedded; 3 option-image Qs (MVCH-195/Q4, ACUR-145/Q71, ACUR-146/Q72 — a/b/c/d order book-verified, answer-figure lands on correct option; Q72 slot-B mislabel fixed). **All 123 six-section solutions written** (EMW+MVCH+EMI+ACUR audit-clean, 0 failed validation; every EMag display_id has solution + answer). **Phase 5 DONE ✅ 2026-06-13** — founder created the solution-diagram folder; 9 solution-side SVGs (book-Q# 17/19/39/`78,79`/81/99/115/116/117 → MVCH-208/210/248/227/228/230 + EMI-107/145/146/147; grouped `78,79.svg` into BOTH MVCH-227 & MVCH-228) embedded (4 into existing 🖼️, 6 created); CDN 200; rollback written. **EMag chapter now COMPLETE end-to-end** (questions + Q-figs + 123 solutions + solution-figs). qmap `physics-mcq-electromagnetism-qmap.md`. **Book Part 5 Ch.4 "Motion of Charged Particles" — COMPLETE end-to-end ✅ 2026-06-13 (all 5 phases, one session).** 40 questions (Q1–40, footer pg 5-145→5-163) **2-way fanned (founder-approved):** **ELST 12** (242–253: charge in pure electric field — projectile/capacitor deflection, time-varying-E, plate kinematics; elst_2 + Q28 elst_1) + **MVCH 28** (252–279: magnetic/crossed-field/Lorentz — mvch_1 force/crossed-field + mvch_2 circular/helical motion). All `Practice`/`['JEE']`/`examDetails:null`/published; validator 40/40; 24 SCQ / 16 MCQ (Type-1 Q1–18 single, Type-2 Q19–40 multi). Read PDF as images; solutions seeded from sidecar `physics-mcq-mocp.md`. **Every answer independently re-derived → all 40 match the book key, 0 corrections, 0 flags, 0 blocking.** **Question figures:** founder manifest (6 SVGs, book-Q# 2/11/15/24/30/35) remapped to display_ids + embedded (all stem figures, no option-images; all 6 visually confirmed vs book). **All 40 six-section solutions** (ELST 253/253 + MVCH 279/279 audit-clean, 0 failed validation; every MOCP display_id has solution + answer). **Solution figures:** 5 SVGs (book-Q# 9/10/11/15/36 → MVCH-258/259/260/264/275) into existing 🖼️ sections; CDN 200; rollback written. qmap `physics-mcq-mocp-qmap.md`. Notion N/A (0 discrepancies). **Book Part 6 Ch.1 "Modern Physics" — COMPLETE end-to-end ✅ 2026-06-13 (NO diagrams; this was the FORMAT v2 teacher-voice PILOT for physics).** 50 questions (Q1–50, footer pg 6-3→6-19) **2-way fanned (founder-approved):** **NUCL 25** (099–123: nuclei/radioactivity/decay-law) + **ATPH 25** (101–125: Bohr/spectra + **X-rays**=tag_atph_5, so DUAL=0; de-Broglie/recoil-in-atom Q40/42→ATPH via later tie-break). All `Practice`/`['JEE']`/`examDetails:null`/published; validator 50/50; 38 SCQ / 12 MCQ. **Every answer re-derived → all 50 match the book key, 0 corrections.** **All 50 solutions written in FORMAT v2** (`format:'v2'`: no `🧠/🗺️/⚡/⚠️` icons, `**Shortcut:**`/`**Watch out:**` labels, 1–2-sentence openings; audit-clean 0-fail). **Workflow + tooling migrated to v2:** `physics-solution-workflow.md`→v3.0 (§🎤 added, §🧱 LEGACY, figure-anchor rule); `apply-batch.js` gained a `format:'v2'` branch + persists `solution.format`; `audit.js` branches on stored format (legacy MVCH/ELST audits still 0-fail — backward compatible). qmap `physics-mcq-modp-qmap.md`, seed `physics-mcq-modp.md`. Notion N/A. **Book Part 7 Ch.1 "Experimental Physics" — COMPLETE end-to-end ✅ 2026-06-13 (FORMAT v2). FINAL CHAPTER — book 100% done.** 20 questions (Q1–20, all SCQ, footer pg 7-3→7-13). **Founder asked to create a new "Experimental Physics" chapter — but `ph12_exp_phy` (prefix EXPP, class 12) ALREADY EXISTED** (and was already wired into insert_questions.js + chapterToSubject); it was empty. Used the existing chapter; **added 2 tags** `tag_expp_8` (Calorimetry/Latent Heat) + `tag_expp_9` (Magnetometer) for Q5/Q20. Display_ids **EXPP-001..020**. validator 20/20. **All answers re-derived → match key EXCEPT Q6 (EXPP-006): book (d) Convex mirror → ingested (c) Concave mirror** (plane-mirror+pin no-parallax measures a concave/real-image element; book's own hint says concave) — **Notion-pushed (Open)**. **Q-figures:** 16 SVGs → 9 Qs incl. 2 option-image Qs (Q9 δ-i graphs, Q14 circuits — a/b/c/d order book-verified ✓). **All 20 v2 solutions** (audit 0-fail). **Sol-figures:** 2 (Q6→EXPP-006, Q10→EXPP-010) into `🖼️` anchors. qmap `physics-mcq-expp-qmap.md`. (NB: `apply-batch.js`/`audit.js` were refactored mid-session into a shared `scripts/lib/solution-validator.js` POLICY module consolidating chem/physics/math validators — EXPP flowed through cleanly.) **Book Part 8 Ch.1 "Miscellaneous Questions—1" — COMPLETE end-to-end ✅ 2026-06-13 (FORMAT v2).** 54 questions (Q1–54, footer pg 8-3→8-18) — a **whole-syllabus mixed set fanned per-question across 20 chapters** (founder-approved): K2D 5, ROPY 6, CAPC 8, MVCH 4, WAVE 4, EMI 3, ROT 3, TDYN 2, THPR 2, NLM 2, COM 2, WVOP 2, SHM 2, ELST 2, CURR 2, GRAV 1, FLUI 1, EMW 1, ATPH 1, ACUR 1. Display_ids appended per-chapter in book order. validator 54/54; 42 SCQ / 12 MCQ. **Every answer re-derived → all 54 match the book key, 0 corrections.** **Q-figures:** founder folder (19 SVGs, NO manifest — mapped by viewing PNGs vs book like CURR) → 15 Qs incl. Q21 4-option-image v-t graphs (a/b/c/d order book-verified ✓). **All 54 v2 solutions** (audit 0-fail spot-checked CAPC/K2D/MVCH). **Sol-figures:** 10 SVGs (book-Q# 3/16/19/22/23/28/29/44/53/54) into `🖼️` anchors; CDN 200. qmap `physics-mcq-misc1-qmap.md`. Notion N/A. **Remaining: Misc Qs-2/3/4 (Part 8) + Part-9 practice papers.** | **⏳ Founder:** **(v2 pilot) review the Modern Physics teacher-voice solutions (NUCL-099..123, ATPH-101..125) — first physics chapter in FORMAT v2; the diff feeds back into `teacher-voice-profile.md`.** **(EMag) review the 3 Notion discrepancies (EMI-150 & EMI-152 Open, EMI-132 Under Review).** Ch.3 question figures DONE; remaining = 14 Ch.3 solution-side figs + confirm Q22→SHM; (carryover) Ch.2 solution-side figures INSERTED ✅ 2026-06-11 (37 Qs); + push 3 Ch.2 verify flags (ROT-223/241/244 → Notion Answer Discrepancies), 3 borderline routings, 13 Rev-Ex-1 diagrams, 4 Ch.1 key reviews. |
| **SEO Dashboard** | 🟡 | PR 1 of 3 landed | 2026-05-25 | Ship PR 2 | — |
| **BITSAT Predictor** | 🟢 | Data layer + predictor + UI/API live. **2026-06-13: Model v4 integration.** Validated the platform's official 2021–2025 cutoffs against the founder's Admissions-Research sheet (`BITSAT_Cutoffs_BranchPredictor_Data.xlsx`) — **0 mismatches across 200 cells**, data clean (regime split 2021=450 / 2022+=390 correct; 3 platform-only rows all legit — Goa B.E. Electronics-and-Computer 2025 + legacy B.Pharm 2021). Replaced the live 2026 projection: was a naive weighted-mean+trend that overshot the expert sheet by +9 to +25 marks; now driven by the sheet's authoritative `low/likely/high` band (new `packages/data/bitsat/predictions2026.ts`, 47 branches). Buckets anchor to the band (Safe=≥high, Target=≥likely, Reach=≥low) with asymmetric-normal probability (high→84%, likely→50%, low→16%); confidence mirrors the sheet (Medium premier / High else). Backtests + legacy regime still use the statistical engine (hindsight QA unaffected). `validate_predictor.ts` 14/14 ✓ (IN-7 rescoped to the statistical path); student tsc 0 errors. **Scheduled: refresh the band on 2026-06-20 from iteration-one updated data.** | 2026-06-13 | **Refresh band on 2026-06-20** from iteration-one data. Optional UI pass: surface bear/base/bull range on result cards (band fields already in the API). Decide whether to add an official 2026 band for Goa B.E. Electronics-and-Computer (currently statistical fallback). | — |
| **Welcome Dashboard** | 🟡 | API shipped, UI pending | 2026-05-22 | Build the UI against the shipped payload | — |
| **Study Planner** | 🟡 | `/study-planner` live (static, 1h ISR; **renamed 2026-06-08 from `/drop-year-planner`** with a 301 redirect — now spans all batches, not just the drop year; SEO title targets "free JEE & NEET study planner, Class 11/12/Dropper"). Multi-mode (Class 11 / Class 12 / Dropper) with per-mode palette, URL `?m=`, one-shot button, Class 12 prereq callout, target-date reverse planner, BYO resources, Crucible practice deep-links. **Phase 5 SHIPPED 2026-06-08** — Physics + Math added to **Dropper** mode (subject switcher in sidebar; all `ph11_/ph12_/ma_` chapters from taxonomy; BYO lectures + Drive notes; Crucible auto-practice; per-subject `currentSubject` state migrated + Zod-validated; dynamic sidebar grouping + dashboard dims). **Refinements same day:** "Smart Plan"→"Study Plan"; physics grouped into PW/Allen modules (Mechanics 1/2, Thermo & Waves, EM, Optics, Modern, Experimental) via `PHYSICS_GROUP_MAP`; redundant "Class N" sub-label hidden for single-class groups; **roadmap day-count now student-editable** (`ModeState.chapterDays` override → `computeRoadmapLayout` re-flows whole timeline + buffer pool dynamically) shown as right-aligned `− N d +` stepper; plan-stat cards got subtle per-stat tints. **Resources reworked 2026-06-09 → batch-aware** (`ChapterPlanItem.resourcesByMode`): Class 11 = detailed lectures + PDF (live from the `/detailed-lectures` sheet via `lectures.server.ts`) + handwritten notes + Crucible; Class 12 = detailed for own chapters, crash courses for Class 11 prereqs; Dropper = crash + DPP + "Revise in 15 min". Inline YouTube player added (lightbox). Handwritten-notes links deep-linked from the notes page's own `crucibleChapterId` metadata. Practice → Crucible topic-wise `?mode=browse`. Mole Concept crash/DPP populated; Telegram CTA (`t.me/mycanvasclasses`) on dashboard + plan. tsc/eslint clean. | 2026-06-09 | **Awaiting founder's crash-course + DPP video links for the remaining dropper chapters** (being uploaded; will share full list or per-chapter — dropper chapters currently fall back to one-shot + handwritten + Crucible). Then: backfill physics/math weightage/difficulty; optional JEE/NEET exam toggle. | **⏳ Dropper crash/DPP videos** — uploading; founder to share links |
| **Livebook image automation** | 🟢 | Pipeline working (ChatGPT→cwebp→R2→block) | ongoing | Use as needed per book | — |
| **Teacher-voice system (solutions)** | 🟢 | **23/23 chemistry chapters distilled + Mole Concept FULLY voice-rewritten (FORMAT v2, 2026-06-12).** All 318 MOLE solutions: blank-line steps, Shortcut/Watch-out labels, no icon headings, founder analogies, compactness rule, banned words gone. Verified 318/318 boxed, 0 icon leftovers, 0 banned words, all 22 audio/video recordings preserved. Re-derived every answer: ~30 null keys filled, 7 new overrides + 9 audit overrides, 5 BLOCKING stem defects (067/076/077/185/276), 8 mis-filed re-route flags, stem typos (082/300/303). 16 new discrepancies pushed to Chemistry Answer Discrepancies Notion DB (Status=Open). **ATOM CHAPTER COMPLETE 2026-06-13 — 295/299 solutions FORMAT v2.** First 15 by hand, then the remaining 273 via a **multi-agent workflow** (20 parallel writers → serial apply → verify): 269 written, 0 missing left, 0 unbraced charges, 0 placeholders, all boxed; +6 pre-existing substandard ones fixed. Re-derived every answer → **5 overrides + 4 BLOCKING stem defects pushed to Chemistry Answer Discrepancies Notion** (9 pages); ~7 audio_flags. Only 4 legacy left = the blocking stems (182/185/235/314), parked for founder. System files: teacher-voice-profile.md, 23 exemplar banks, chemistry-solution-workflow §🎤 v4.0, BOOK_PAGE_WORKFLOW §5.V, apply-batch validator (format:'v2' + lint + banned words + `\ce` charge auto-normalize). **2026-06-13 tooling: validators CONSOLIDATED** — extracted `scripts/lib/solution-validator.js` (shared `validateSolution`/`normalizeCeCharges`/`POLICY`); all 6 toolkit files import it, 0 local copies remain. Fixes the drift root cause (a physics-pilot agent had caught `chemistry-solutions/apply-batch.js` still hard-requiring the 4 icons). Per-subject intent preserved via POLICY (physics allows 🖼️+"anchor"; chem/math forbid icons + ban anchor; math-only crammed SOFT lint). Verified 20/20 unit + 3 audits + dry-run + node --check. | 2026-06-13 | **Founder (ATOM):** fix the 4 blocking stems (ATOM-182/185/235/314) + confirm the 5 overrides (052/115/177/179/218) in Notion. **Founder (MOLE):** fix 5 blocking stems + re-route 8 + record audio. **Next chapter:** pick PERI or BOND — 'voice-rewrite [chapter] per chemistry-solution-workflow §🎤'. (Validation rule change → edit the shared module + the right POLICY entry, never re-copy a validator into a toolkit.) | — |
| **Project Ledger (this system)** | 🟢 | Cockpit + CLAUDE.md rituals + SessionStart hook + weekly review routine (Mon 09:08 IST) all live | 2026-06-06 | — (complete; tune cadence/format as needed) | — |
| **Structure Editor (ChemDraw-like)** | 🟡 | **BUILT 2026-06-14.** Admin authoring tool at `admin.canvasclasses.in/structure-editor` — a ChemDraw-class drawing canvas via **Ketcher** (EPAM, Apache-2.0, runs fully in-browser on its standalone Indigo-WASM engine; no backend). **Scope (founder, 2026-06-14): pure organic-structure DRAWING only** — no biomolecules (macromolecule mode disabled via `disableMacromoleculesEditor`), no SMILES/chemistry-data, no spectroscopy. Use case = redrawing blurred question-source figures into sharp, clean lines. Draws open/closed chains, rings, functional groups, stereochemistry. Export toolbar is image-only: **Download SVG** (vector, infinitely sharp), **Download PNG**, **Push-to-R2** (reuses the existing `/api/v2/books/assets/upload` route → hosted URL). SMILES/MOLfile/InChI outputs + the openchemlib preview were removed per founder; route First-Load dropped 580→230 kB. **All element symbols forced BLACK** (toolbar + canvas) by blanking ketcher-core's shared `ElementColor` map (no CPK colours). **Name→Structure SHIPPED 2026-06-14:** "Generate from name" sidebar input → admin route `POST /api/v2/iupac/name-to-structure` (proxies OPSIN @ EBI `…/opsin/ws/<name>.smi`, admin-gated, SSRF-safe fixed host) → `ketcher.setMolecule(smiles)` draws it. Verified vs OPSIN (benzene/2-methylbut-2-ene/cyclohexanone/etc all correct; gibberish→friendly error). **Structure→Name DEFERRED (founder decision 2026-06-14):** no free reliable offline engine exists (RDKit/OpenBabel/CDK don't do it; only paid ChemDraw/ACD/ChemAxon or best-effort AI) — skipped to keep the tool trustworthy. **2026-06-15 — 3 more features SHIPPED:** (1) **Dark/light export toggle** — "Dark page" recolours the exported SVG black→white + strips the bg so structures drop onto dark book/Crucible pages (Indigo only renders black, so recolour is done in-code on the SVG string); "Light/print" = black. (2) **Trace-over reference image** — load/paste a blurry source figure as a faint pointer-events:none overlay over the canvas (opacity slider + show/hide + clear) to trace a clean structure over it. (3) **Copy PNG to clipboard** + PNG rasterised from SVG at 2x for crispness. All export paths (SVG/PNG/Copy/R2) are theme-aware and selection-aware. **Crucible image-storage Q answered:** question figures already store per-question at R2 key `questions/<uuid>/svg/…` (single bucket `canvas-chemistry-assets`), Asset doc links `used_in.questions`, referenced as markdown `![](url)`; chapter lives in question metadata, not the path. Direct-attach-to-question (feature 3) deferred pending founder decision (full attach w/ question-picker vs hosted-link-only). **2026-06-15 — 2 more:** (a) **Reference image resize/move** — size slider (10–150%, default 60%), opacity, a Move toggle (drag to reposition, pauses drawing), show/hide, clear (fixes pasted images covering the whole canvas). (b) **Functional-group quick-insert buttons** (NO₂, COOH, CHO, C=O, CN, OH, NH₂, OMe, COOMe, CONH₂, SO₃H, Ph, F, Cl, Br, I) via `ketcher.addFragment(smiles)` — drops the group on the canvas to bond in. **Why buttons not Ketcher's native FG library:** the library's data files (`ketcher-fg-tmpls.sdf` etc.) are build-generated, NOT shipped in the npm package nor committed in epam/ketcher, and the demo CDN 403s — so Ketcher's Functional-Groups template tab is empty in our standalone embed (`staticResourcesUrl=""`). Self-contained buttons sidestep that. Files: `FunctionalGroups.tsx`, `ReferenceImageControl.tsx`, `StructureEditorClient.tsx`, `ExportToolbar.tsx`. **2026-06-15 (later) — FG buttons upgraded to NATIVE behaviour:** discovered Ketcher's full functional-group library (62 entries incl. NO₂ as a proper collapsed `SUP` abbreviation w/ charges + `SAP` attachment point) is **embedded in the ketcher-react bundle** (the un-shipped SDF is only an override). Extracted 20 common groups' authoritative molfiles → `functionalGroupTemplates.ts`; buttons now arm Ketcher's template tool via `editor.tool('template', {struct})` (struct from `MolSerializer.deserialize`, exposed through `KetcherApi.molfileToStruct`) → click button then click a bond end/atom = literature abbreviation + auto-snap + valence balancing (fallback `addFragment` still drops the collapsed abbreviation). Native Templates→Functional Groups dialog also works (embedded). Earlier plain-atom `addFragment(SMILES)` buttons replaced. Files: `apps/admin/app/structure-editor/page.tsx` (AdminPanel gate super) + `apps/admin/features/admin/structure-editor/{StructureEditorClient,KetcherCanvas,ExportToolbar}.tsx`; landing card on `app/page.tsx`; CSP `worker-src 'self' blob:` + webpack `jsdom/canvas` alias-false (Paper.js) in `next.config.ts`. Ketcher code-split behind `dynamic(ssr:false)` — never touches SSR or other routes. **`npm run build:admin` passes clean (exit 0).** **DEPLOY STATUS (root cause found 2026-06-15 via Vercel log):** on `origin/main` but **NOT live** — the `canvas-admin` Vercel build is **OOM-killed** (build container runs out of RAM building the heavy editor bundles Ketcher + Excalidraw); NOT a code error (lint/types pass, warnings only). Student app `canvas-classes-website` deploys fine. **Fix applied:** `experimental.webpackMemoryOptimizations: true` in admin `next.config.ts` (commit `5f11823`, on PR branch). **2026-06-19 — LOCAL-RUN FORK BUILT (founder chose the offline/local path).** Both editors ported into a standalone, NOT-deployed Vite+React app at `tools/canvas-draw-studio/` (lives outside `apps/*` + `packages/*` so it can never enter a Vercel build → the OOM blocker is sidestepped entirely; there is no production build). Download-to-disk only — **Push-to-R2 removed**; name→structure calls EBI OPSIN directly (online-only, everything else works offline); Excalidraw fonts bundled locally (`public/excalidraw-assets/`) for offline rendering. Run = double-click `Start.command` → zero-dependency Node static server (`serve.mjs`) → browser; no install step (committed `dist/`). `npm run build` green in ~22s (Vite, no OOM); `tsc --noEmit` clean; server verified serving HTML+bundle+fonts. On branch `feat/local-draw-studio`, **not yet committed**. **✅ VERIFIED WORKING IN BROWSER 2026-06-19** (drove the built app via preview MCP): Ketcher boots, draws (benzene/ethanol via setMolecule), and the Download-SVG export path produces a valid SVG; Excalidraw boots, **58 library shapes built**, accepts shapes, Download-SVG produces an `image/svg+xml` file; zero runtime errors. Required **3 Vite-vs-Webpack fixes** Ketcher needs (admin's Webpack auto-handled them): (1) `build.commonjsOptions.transformMixedEsModules` — ketcher-core's ESM build has a stray `require("raphael")`; (2) `define: { global: 'globalThis' }` — a Ketcher dep refs Node's `global`, which threw inside the engine-init promise and silently blocked `onInit`; (3) `optimizeDeps.include` of the ketcher pkgs for dev. | 2026-06-19 | **Founder smoke-test the local app** (double-click `Start.command`, draw + export in both editors). Then decide: does the local tool REPLACE the web-deploy path (if so the OOM PR `chore/remove-interactive-graph` can be dropped/closed) or run alongside it? **Commit `feat/local-draw-studio` once approved.** | **Not blocked** — local version runs with no deploy. (Web-deploy via the OOM PR remains open if the founder also wants the editors live on admin.) |
| **Math graph maker (JSXGraph)** | 💤 | **SHELVED / REMOVED 2026-06-15 (founder decision).** Built a JSXGraph math grapher — a Live Books `interactive_graph` block (self-service form: axes + functions + draggable sliders) + a standalone `/graph-editor` admin tool + prebuilt named graphs (tangent / area-under-curve / reflection). Founder judged it incomplete/unimpressive → **fully removed** (block, standalone tool, `jsxgraph` dep, all wiring) via branch `chore/remove-interactive-graph` (commit `4104373`, PR open). **Decision + rationale preserved for the future rebuild:** engine choice was JSXGraph (MIT, self-hosted) over GeoGebra (paid commercial licence) and Desmos (hosted, own terms); square-board + `keepAspectRatio` needed for symmetric axes; JSXGraph ships uncompiled source so prefer aliasing to its prebuilt core. | 2026-06-15 | **Future: build a dedicated math graph-maker tool separately**, then integrate into Live Books only if it proves solid. Revisit JSXGraph vs a from-scratch SVG approach. | — (shelved by choice; nothing blocking) |
| **Diagram Editor (physics/math)** | 🟡 | **Phase 1 (editor shell) BUILT 2026-06-15.** Admin tool at `admin.canvasclasses.in/diagram-editor` for physics/math question figures (pulleys, wedges, blocks, circuits, optics, thermo graphs). Built on **Excalidraw** (MIT, React, embeds like Ketcher; chosen over tldraw = $6k/yr license, drawio = no mechanics/optics + clunkier). Gives full editor UX free + per-shape precise↔hand-drawn ("sloppiness") + native dark/transparent export. Export sidebar = SVG/PNG/Copy/Push-to-R2 (reuses `/api/v2/books/assets/upload`), selection-aware, Dark-page/Light-print toggle via Excalidraw's `exportWithDarkMode`. Excalidraw loaded `dynamic(ssr:false)`; fonts from jsdelivr via `EXCALIDRAW_ASSET_PATH` (added jsdelivr to admin CSP connect-src). Files: `apps/admin/app/diagram-editor/page.tsx` + `features/admin/diagram-editor/{DiagramEditorClient,ExcalidrawCanvas,DiagramExportToolbar}.tsx`; landing card added. `build:admin` clean (exit 0; note: corrupted `.next` cache caused phantom PageNotFound — build into fresh `NEXT_DIST_DIR` to bypass). | 2026-06-15 | **Founder: browser smoke test** (`npm run dev:admin`, port 3001) — draw + export; confirm Excalidraw fonts load (CSP) + dark-page export gives light lines on transparent. Then **Phase 2: author custom shape libraries** — Mechanics (pulleys/wedge/block/spring/vectors/ground) → Circuits → Optics → Graphs/Thermo, as Excalidraw library items (the bulk of the work; ~40 symbols, iterative). **Phase 2 (first pass) BUILT 2026-06-15:** 23 shapes preloaded into the editor's library panel via `initialData.libraryItems` (built from skeletons with `convertToExcalidrawElements` + `restoreLibraryItems`, shared groupId per shape, roughness 0). Mechanics: pulley, block, wedge/incline+ground, spring, force-vector, ground-support. Circuits: resistor, capacitor, cell, bulb, switch, ammeter, voltmeter. Optics: convex/concave lens, plane mirror, concave mirror, principal axis (dashed), object arrow. Graphs: x–y axes, P–V axes, P–V cycle, isotherm. Files: `features/admin/diagram-editor/diagramShapes.ts` (geometry/registry) + library build in `ExcalidrawCanvas.tsx` (defensive per-shape; bad skeleton can't break the editor). `build:admin` clean (exit 0, fresh NEXT_DIST_DIR). **2026-06-15 (later) — richer palette + bigger library (founder feedback: toolbar too sparse, no XY graph, library thin):** Excalidraw's native toolbar is NOT extensible, so added a **left categorized + searchable shape palette** (`ShapePalette.tsx`) = one-click insert onto canvas via `updateScene` + `scrollToContent` (bridge `insertShape` from `ExcalidrawCanvas`). Library expanded ~23→**50 shapes** incl. the missing **X–Y axes / 4-quadrant / grid**, number line, straight-line/parabola, V–T & P–T axes, adiabat, movable pulley, stacked blocks, incline-θ, vertical spring, velocity vector, wall, hanging mass, lever; circuits +zigzag-resistor/battery/galvanometer/inductor/junction/ground/rheostat; optics +convex mirror/ray/prism/eye. **2026-06-15 (later still) — founder feedback: the left text palette duplicated Excalidraw's visual Library panel → REMOVED `ShapePalette.tsx` + insert bridge; layout back to canvas+export, shapes live only in the native Library panel (50 items preloaded).** `build:admin` exit 0. **Path forward agreed: GROW the library from real diagrams the founder shares** — author each as an Excalidraw library item (lines/circles/arrows/text via skeleton→`convertToExcalidrawElements`). Limits: curves approximated by multi-segment lines (no true arc primitive); no photo/shaded art. Also viable: founder draws + Excalidraw "Add to library" (bake into default); import community `.excalidrawlib` (e.g. circuits). **DEPLOY STATUS (root cause found 2026-06-15):** on `origin/main` but **NOT live** — same cause as the Structure Editor: the `canvas-admin` Vercel build is **OOM-killed** (heavy Ketcher+Excalidraw bundles), not a code error. Fix `webpackMemoryOptimizations` pushed (commit `5f11823`). **2026-06-19 — INCLUDED IN THE LOCAL-RUN FORK (founder chose offline/local path).** The Excalidraw diagram editor (all 50 library shapes) is ported into the standalone, NOT-deployed Vite app at `tools/canvas-draw-studio/` alongside the Structure Editor — runs locally via double-click `Start.command`, no Vercel build, no OOM. Push-to-R2 removed (download-to-disk only); Excalidraw fonts bundled locally for offline rendering. See the Structure Editor row for the full local-fork detail. On branch `feat/local-draw-studio`, not yet committed. **✅ VERIFIED WORKING IN BROWSER 2026-06-19**: Excalidraw boots in the Vite build, 58 library shapes load, canvas accepts shapes, Download-SVG produces an `image/svg+xml` file, zero errors. | 2026-06-19 | **Founder smoke-test the local app** (draw + export; confirm the 50-shape Library panel + dark-page export). Decide local-vs-deploy (see Structure Editor row). Then keep growing the shape library from real diagrams. | **Not blocked** — runs locally without deploy. (Web-deploy via the OOM PR still open if also wanted live.) |
| **Live Book Content Protection** | 🟡 | **Phases 1 + 1.5 SHIPPED 2026-06-14.** Phase 1.5: soft-delete query middleware on `BookPage`+`Book` models (one place hides deleted content from every read; existing pages stay visible since Mongo treats missing==null); shared `@canvas/data/books/page-protection.ts` snapshot helper; admin page DELETE→soft+snapshot, page PUT→snapshot-before-overwrite, book DELETE→soft. Both apps typecheck clean. **Phase 1 SHIPPED 2026-06-14** after a silent hard-delete of a section + the founder's video (2026-06-10, unnoticed till 06-14). Built `scripts/lib/book-writer.js` (sole mutation gateway: version-snapshot → content-loss guard → audit → soft-delete; `restorePageVersion`); `book_page_versions`+`book_audit` collections; **baseline snapshot of all 672 pages (full doc)**; CLAUDE.md §0.6 + BOOK_PAGE_WORKFLOW rule (no hard-delete/silent drop; gateway-only). Guard self-tested 4/4. Full design: `_agents/plans/CONTENT_PROTECTION.md` (6 layers). | 2026-06-14 | **Phase 2:** asset registry (`book_assets`) + orphan/broken-link detection; then structural blueprint + daily drift watchdog/alerts; Phase 3 scheduled DB/R2 backups + dry-run/digest; make unique indexes partial on `deleted_at:null`; restore/trash UI. | — |

| **AI-Native Company (Canvas Brain + watchdogs)** | 🟡 | **Strategy agreed 2026-06-18, nothing built.** The thread to move Canvas from "AI-assisted" (AI = fast employee) to "AI-native": (A) a **standing supervisory layer** — scheduled watchdog agents (content guard, cost/perf sentinel, gap auditor, student-signal analyst) that watch the whole system and report, directly preventing repeats of the June-2026 Vercel bill spike + the June-10 silent content+video deletion; (B) **one "Canvas Brain"** institutional memory with **three faces** — founder advisor (suggest improvements + morning brief), student tutor (answers in our documented voice, **retrieval-grounded + cited**, not fine-tuned), navigator (search the growing library) — fed by content (books/Qs/solutions + video/lecture transcripts), operations (PROJECTS/ADRs/incidents/watchdog findings), and an **industry scout** (uses the already-paid vidiq MCP + web for first-mover advantage). Kept separate from the founder's personal GBrain. Key correction: "continuously trained" → **retrieval**, not fine-tuning (instant, cited, cheap, safe). Security guardrails written (read-and-report by default; per-face data-leakage isolation via existing RBAC/tenancy; treat ingested content as data not instructions; SSRF rules for the scout; cost caps). Full detail: `_agents/plans/AI_NATIVE_ROADMAP.md`. **Runtime decision 2026-06-18: internal phases run on the founder's Claude Max plan via agent sessions — NO API/endpoint until student-facing; API decision deferred to the tutor/navigator phase.** **BUILT 2026-06-18:** (1) **Canvas Brain substrate** = repo-resident git-diffable institutional memory `_agents/brain/` (BRAIN.md index + company-overview + systems-map + incidents + how-to-update; pointer-based, mirrors the NCERT corpus). (2) **Founder advisor face** = `_agents/workflows/FOUNDER_ADVISOR.md` + `/founder-advisor` skill — reads the brain+cockpit+live system, sweeps the joints (caching/auth/content-protection/stalled-deps/cross-system gaps), returns a prioritized grounded brief; read-and-report only, runs on Max. **2026-06-18 (cont.): content-guard watchdog SHIPPED** — first Layer-A agent `scripts/watchdogs/content-guard.js` (+ `restore-helper.js`, `watchdogs/README.md`): read-only, compares each live Book page to its latest `book_page_versions` snapshot, classifies tracked (gateway-approved, 🟡) vs **untracked** (June-10 signature, 🔴) loss, exit-code alerting. **Verified clean against 673 live pages / 673 snapshots, exit 0.** **Also: cleaned up 6 pre-§0.6 destructive book scripts** (add_ch1_p13_p14_oleum_h2o2, kaveri_ch1_practice_page, kaveri_reading_skills, merge_homogeneous_heterogeneous, science-ch6/build_remaining_pages, setup_class9_hindi_apathit) — each now `require('scripts/lib/legacy-book-guard.js')` which refuses to run (exit 1, before any Mongo) unless `ALLOW_LEGACY_BOOK_SCRIPT=1`, redirecting authors to `book-writer.js`; the other ~5 grep hits were comment-only (verified, untouched). **2026-06-18 (cont.): ENTIRE supervisory layer + scout SHIPPED.** All Layer-A watchdogs built in `scripts/watchdogs/` (read-only, exit-code alerting, all verified live): **content-guard** (clean 673/673), **cost-sentinel** (clean), **gap-auditor** (found **55 orphan chapter_id + 828 dangling tag_id**; 4,176 missing-solution reported as backlog metric), **student-signal** (sparse), + **morning-brief** orchestrator (aggregates all four, worst-severity exit — verified end-to-end). **Industry scout** built as `/industry-scout` (agent-run on Max: vidiq MCP + web → `_agents/brain/industry/`). Brain index/systems-map/incidents all updated. **2026-06-18 (cont.): orphan-chapter cleanup DONE** (`scripts/fix_temp_orphan_questions.js`, rollback saved) — 42 orphan `TEMP-*` soft-deleted (chapters missing from taxonomy), 6 moved to real chapters, 7 `AROM-*` → `ch11_hydrocarbon`; **orphan chapter_id 55 → 0** (verified); CRUCIBLE_STATE + brain updated. **Also improved `BRAIN.md` with a task-router** so a single-area chat reads only its lane. | 2026-06-18 | **(1) ✅ DANGLING-TAG CLEANUP COMPLETE — bank dangling 828 → 0 (gap-auditor severity 0, morning-brief all-green).** All chem chapters retagged (haloalkanes 124, carbonyl 195, goc 145, pblock12 103, prac_org 102, pblock11 53, amines 28, + tails) via `scripts/fix_chapter_dangling.js` (per-chapter `_cfg_*.json` + rollbacks); **5 new primary tags** added where real taxonomy gaps existed. ⚑ Generic catch-all maps (ch12_pblock `tag_pblock_1`) are baselines → **refine into specific families in the micro-tagging pass**. **(2) ✅ Decided 2026-06-19: NOT adding the 7 "missing" chem chapters (not in latest NCERT) → 42 soft-deleted TEMP PYQs stay permanently deleted; do not restore.** **(3) Cost cleanup DONE 2026-06-19** — 6/7 Vercel-cost backlog items fixed (#16,17,19,20,21,22) + flag-resolve bust dropped; cost-sentinel extended to guard them; #18 per-chapter revalidateTag deferred (needs read-side cache-factory; shows 🟡). **(4) Schedule** `morning-brief.js` daily (launchd command handed to founder). **(5)** Later: transcript feed → navigator → tutor (API decision). Optional: micro-tag refinement of the 2 baseline clusters; consolidate GOC SN1/SN2 → haloalkanes. | Open: brain storage at scale; transcription (wave 2); student-facing faces deferred to the API decision. |

## ⛔ Waiting-on-a-dependency (the things most easily forgotten)

These are *built or partly built* but stalled on something external. When the dependency lands, the linked work can resume — **this section exists so that "I forgot X was waiting on Y" can't happen.**

- **Adaptive Persona Engine → AI tutor needs Anthropic credit to go live.** Phase 2 (persona-snapshot + guardrailed AI tutor + model routing) is BUILT and verified up to the live model call. The live round-trip 400s on "credit balance too low" — the SDK wiring, API key, model id, and request shape all authenticate fine. **Action: top up the Anthropic account, then ask the agent to re-run the tutor live smoke test.** No code change needed.
- **Adaptive Persona Engine → cross-surface content dependency REMOVED (2026-06-07, ADR-012).** The engine no longer needs lecture/audio + `ResourceLink` rows to deliver value — Live Books are decoupled and the engine is re-centred on in-Crucible adaptive practice. The `ResourceLink` "read/watch this" path is **parked, not deleted** (dormant, revivable). The real remaining work is content-internal to Crucible: finish per-chapter micro-tagging + difficulty + self-sufficient solutions for 2–3 chemistry chapters, which then unblocks the **question grouping / variant-skipping** flagship (its own `variant_group_id` tag + design spec, TBD).
- **Class 9 Math Part II → needs NCERT PDFs** for DB chapters 9–15.
- **Class 9 English audio → needs recording** (schemas ready; all `audio_url` empty).
- **Crucible Physics/Maths micro-tagging → needs taxonomy micro_topics generated** for those subjects.
- **AI-Native Company (Canvas Brain) → any live LLM face needs Anthropic credit topped up** (same blocker as the Adaptive Persona tutor). The text-only brain substrate + founder advisor can be built and dry-run before the top-up; only the live answering call is blocked.

## Where the detail lives (don't duplicate — link)

| Initiative | Detail doc |
|---|---|
| Adaptive Persona Engine | `_agents/plans/UNIFIED_LEARNER_PERSONA.md` + ADR-011 + **ADR-012 (decoupling, 2026-06-07)** + GBrain `decisions/2026-06-06-unified-learner-persona.md` |
| Live Books (all) | `_agents/state/LIVE_BOOKS_STATE.md` (auto-generated content inventory) |
| Hindi गंगा CBSE exam alignment | `_agents/plans/HINDI_CBSE_EXAM_ALIGNMENT.md` (blueprint + gap analysis + build plan) |
| Crucible bank | `_agents/state/CRUCIBLE_STATE.md` |
| Math / ICT books | `_agents/state/MATH_BOOK_BUILD.md`, `ICT_BOOK_BUILD.md` |
| SEO | `_agents/plans/SEO_DASHBOARD.md` + `_agents/SEO_PLAYBOOK.md` |
| BITSAT / Welcome | `_agents/plans/BITSAT_PREDICTOR_INTEGRATION.md`, `WELCOME_DASHBOARD_PLAN.md` |
| Study Planner (was Drop Year Planner) | `docs/superpowers/specs/2026-06-07-drop-year-planner-design.md` (build spec) |
| AI-Native Company (Canvas Brain + watchdogs) | `_agents/plans/AI_NATIVE_ROADMAP.md` |
| Tech debt | `_agents/DEEPENING_BACKLOG.md` |

## Standardised status header (for every plan/design doc)

So a project's detail doc tells you its state in 5 seconds, each `_agents/plans/*.md` carries this block at the top (the cockpit row is just its one-line summary):

```
> **Status:** 🟡 <one line> · **Last updated:** YYYY-MM-DD
> **Done:** <bullet or one line>
> **Pending:** <what's left>
> **Blocked on:** <dependency, or —>
> **Next action:** <single concrete next step>
```

`_agents/plans/UNIFIED_LEARNER_PERSONA.md` is the worked example.
