# Diagram wishlist — Physics MCQ (Deb Mukherji) Ch.2 "Circular, Rotational & SHM"

Two lists. **List A** = figures the QUESTION itself needs (upload to the question via admin). **List B** = figures the book drew inside its worked SOLUTION (upload to the solution's 🖼️ Visual Sketch once Phase-4 solutions are written). Created 2026-06-10 at ingestion. display_id ↔ book Q# in `_agents/solution-seeds/physics-mcq-ch2-qmap.md`.

---

## List A — QUESTION-SIDE figures (upload to the question)

> **✅ ATTACHED 2026-06-11** — all 7 List-A REQUIRED + the 7 optional question figures (NLM-185, SHM-122/124, ROT-226/236/240/246) were attached via the `figure-inserter` skill (`scripts/svg-mapper/`, manifest in the "Circular, rotational & SHM" folder). 14 SVGs → 14 questions, all stems. Only List B (solution-side) remains.

These questions reference a figure whose geometry is NOT fully recoverable from the text alone — **REQUIRED** before students can answer them well.

| display_id | book Q | what the figure shows |
|---|---|---|
| **NLM-188** | Q11 | Uneven horizontal road profile with labelled points A (dip/valley), B (crest/hump), C (dip) — the question asks where N is max and the points are only defined by the figure. |
| **NLM-190** | Q13 | Quarter-circle tube AC in a vertical plane (A top, C bottom), ball B inside; needed to read inner/outer wall orientation. |
| **NLM-201** | Q58 | Particle P on a vertical axis held by two strings AP, BP (length l each, AB = l ⇒ equilateral); P rotating. (Text mostly specifies it — recommended, not strictly blocking.) |
| **NLM-205** | Q62 | S-shaped smooth track ABCDE in a vertical plane: ABC and CDE are quarter circles, B & D very close to C; magnet M starts at A. |
| **ROT-242** | Q88 | Rolling ring with marked points/sections: B top, D bottom (contact), A left, C right — the KE-of-sections comparison is unreadable without it. |
| **ROT-244** | Q90 | Disc D rolling on surface S, string wound on D up over a massive pulley P to a hanging block B. |
| **ROT-245** | Q91 | Two blocks m₁ > m₂ hanging over a massive pulley P (radius r); m₁ accelerates down. |

---

## List B — SOLUTION-SIDE figures (upload to the solution's 🖼️ section, Phase 4)

> **✅ ATTACHED 2026-06-11** — 34 solution-side SVGs (the "Ch 2 Solutions Diagrams" folder, book-Q#-named) → 37 questions' solutions via `figure-inserter`/svg-mapper. 26 dropped into the existing 🖼️ Visual Sketch section; 11 solutions had no 🖼️ section so one was created (after 🧠, before 🗺️) holding the figure — **those 11 have no caption yet; an optional re-pass could add a one-line description now that the figures are legible.**

> **✅ SOLUTIONS WRITTEN 2026-06-11** — all 100 Ch.2 teacher-style solutions are now in the DB (NLM/ROT/SHM/COM). Every solution below already has a `🖼️ Visual Sketch` section describing the figure in prose; these List-B diagrams are now **ready to upload** to enrich those sections. Until uploaded, the prose sketch stands on its own.

The book printed a diagram inside the worked solution for each of these. Most are simple FBDs / geometry sketches. Upload to enrich the 🖼️ section of the (now-written) teacher-style solutions (the seed in `physics-mcq-ch2.md` describes each).

**NLM (circular dynamics)**
- NLM-179 / NLM-180 / NLM-181 (Q2,3,4) — vertical circle: circle with A top, B bottom, C centre, D side; v₁/v₂/v₃, T₁/T₂/T₃, mg.
- NLM-184 (Q7) — rod/pendulum released from top, swings to angle θ, T=0 marked.
- NLM-187 (Q10) — plumb-bob FBD: T cosθ, T sinθ, mg, radial accel a_r.
- NLM-189 (Q12) — hollow cylinder with particle on inner wall, N, v.
- NLM-192 (Q15) — car wheels: O (centre of curvature), C (CM), N₁/N₂ up, F₁/F₂ friction.
- NLM-193 / NLM-194 (Q16,17) — banked surface: N, N cosθ, N sinθ, mg, angle θ; railway gauge d, height h.
- NLM-195 (Q18) — cyclist leaning at 90° to a banked road.
- NLM-197 (Q54) — pendulum: angular amplitude φ, instantaneous angle θ, L, T, mg.
- NLM-199 (Q56) — conical pendulum: θ, L, r = l sinθ, T cosθ, T sinθ, mg.
- NLM-200 (Q57) — vertical circle: v_i = u (bottom, horizontal), v_f (side, vertical), l.
- NLM-202 (Q59) — rotating rod, element at x, width dx, tension T and T+dT.
- NLM-203 (Q60) — rotating liquid tube, element at x, dx, force F and F+dF.
- NLM-204 (Q61) — rotating ring, small arc subtending 2θ at centre C, T cosθ / T sinθ.
- NLM-205 (Q62) — same ABCDE track as List A (one figure serves both).
- NLM-206 (Q65) — perpendicular a_t and a_r = v²/r, resultant a, angle θ.

**ROT (rotational)**
- ROT-195 (Q25) — metre scale, T₁/T₂ at ends, mg @0.5 m, 2mg @0.75 m.
- ROT-196 (Q26) — two like parallel forces F₁, F₂, resultant F; and the interchanged case.
- ROT-198 (Q28) — light scale, weights g@1cm, 2g@2cm, …, support N@l.
- ROT-200 (Q30) — block a×a×h, C centre, N up, F friction left, mg, motion arrow.
- ROT-208 (Q38) — rod at angle θ to axis through one end, element x, perpendicular x sinθ, dx.
- ROT-209 (Q39) — annular disc, elemental ring radius x, width dx, inner r outer R.
- ROT-210 (Q40) — disc with X, Y (in-plane) and Z (perpendicular) axes.
- ROT-212 (Q42) — equilateral triangle of rods, axis through corner A ⊥ plane, median I_D, h, 60°.
- ROT-213 (Q43) — square plate, central axes AB & A′B′ and CD & C′D′ at angle θ.
- ROT-227 (Q73) — uniform beam A–B, men at A & B, C centre, N up @A, mg @C.
- ROT-228 (Q74) — rod hinged at foot A, fallen to angle θ, point P, C, accel a, mg.
- ROT-231 (Q77) — disc (I₁, spinning ω₁) with a ring (I₂) dropped on top; m, r.
- ROT-232 (Q78) — two discs of different radii with rims in contact.
- ROT-234 (Q80) — sphere A (impulse J through centre) and sphere B (J at height h off-centre).
- ROT-239 (Q85) — rolling sphere approaching a wall: v, ω, α, contact friction F.
- ROT-241 (Q87) — rolling ring, centre C speed u; points B(top), A(bottom), D(side), E; 60°.
- ROT-243 (Q89) — wheel, blob leaves top D with 2v at height 2r, lands B; contact A; trajectory.
- ROT-249 (Q95) — rod hinged at top, m, C centre, impulse J at the bottom, length l.

**SHM**
- SHM-127 (Q99) — coin at displacement x from mean O, friction F, N, mg.
- SHM-128 (Q100) — coin on a vertical-SHM platform, positions I (bottom) & II (top), N, mg, a.

**Optional (book drew a small sketch but the text is self-contained — nice-to-have):**
NLM-185 (Q8 spring+mass), ROT-226 (Q72 block on incline), ROT-236 (Q82 disc A→B), ROT-240 (Q86 sphere on plank), ROT-246 (Q92 A-B-P rod), SHM-122 (Q52 spring blocks), SHM-124 (Q96 spring-block in field).
