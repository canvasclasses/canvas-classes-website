// THROWAWAY dev test page (private folder, not routed). SAFE TO DELETE:
//   rm -rf apps/student/app/simtest apps/student/app/_simtest
import { notFound } from 'next/navigation';
export default function SimTest() {
  notFound();
}
