# admin

Empty scaffold. The admin Next.js app will be populated in Phase 5 of the
monorepo migration, when `/crucible/admin/*` routes and admin-only API
routes move out of `apps/student/`.

Until then this directory exists only to register `admin` as a workspace.
